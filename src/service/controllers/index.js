"use strict";

const ArticlesController = require(`./articles_controller`);
const CategoriesController = require(`./categories_controller`);

module.exports = {
  ArticlesController,
  CategoriesController,
};
